# =============================================================================
# CONFIGURACIÓN DE KEYCLOAK - VARIABLES DE ENTORNO
# =============================================================================
# Este archivo contiene las variables de entorno necesarias para ejecutar
# Keycloak con docker-compose.
#
# INSTRUCCIONES:
# 1. Copia este archivo a .env: cp .env.example .env
# 2. Modifica los valores según tu entorno
# 3. NO commitees el archivo .env con credenciales reales
# =============================================================================

# -----------------------------------------------------------------------------
# Versión de Keycloak
# -----------------------------------------------------------------------------
# Versión de la imagen de Keycloak a utilizar
# Recomendado: Usa versiones LTS o estables
KEYCLOAK_VERSION=21.1.1

# -----------------------------------------------------------------------------
# Nombre del Contenedor
# -----------------------------------------------------------------------------
# Nombre personalizado para el contenedor de Keycloak
# Útil para identificar el contenedor en docker ps
KEYCLOAK_CONTAINER_NAME=keycloak

# -----------------------------------------------------------------------------
# Credenciales de Administrador
# -----------------------------------------------------------------------------
# ⚠️ IMPORTANTE: Cambia estas credenciales en producción
# Usuario administrador de Keycloak (acceso a la consola de administración)
KEYCLOAK_ADMIN=admin

# Contraseña del usuario administrador
# ⚠️ SEGURIDAD: Usa una contraseña fuerte en producción
# Recomendación: Mínimo 16 caracteres con mayúsculas, minúsculas, números y símbolos
KEYCLOAK_ADMIN_PASSWORD=admin

# -----------------------------------------------------------------------------
# Puertos
# -----------------------------------------------------------------------------
# Puerto externo donde estará disponible Keycloak
# Formato: PUERTO_HOST:PUERTO_CONTENEDOR
# El puerto del contenedor (8080) no debe cambiarse
KEYCLOAK_PORT=8180

# -----------------------------------------------------------------------------
# Rutas de Volúmenes
# -----------------------------------------------------------------------------
# Ruta local donde se encuentran los archivos de importación del realm
# Esta ruta debe contener el archivo realm-logistica.json
KEYCLOAK_IMPORT_PATH=./keycloak/import

# =============================================================================
# NOTAS ADICIONALES
# =============================================================================
# 
# 1. MODO DESARROLLO vs PRODUCCIÓN:
#    - Este docker-compose usa "start-dev" que es SOLO para desarrollo
#    - Para producción, cambia el comando a "start" y configura:
#      * Certificados SSL
#      * Base de datos externa (PostgreSQL)
#      * Reverse proxy
#
# 2. IMPORTACIÓN DEL REALM:
#    - El flag --import-realm importa automáticamente realm-logistica.json
#    - El archivo debe estar en la ruta especificada en KEYCLOAK_IMPORT_PATH
#
# 3. PERSISTENCIA:
#    - En desarrollo se usa H2 (base de datos en memoria)
#    - Los datos se pierden al detener el contenedor
#    - Para persistencia, configura una base de datos externa
#
# 4. ACCESO:
#    - Consola admin: http://localhost:${KEYCLOAK_PORT}/admin
#    - Credenciales: KEYCLOAK_ADMIN / KEYCLOAK_ADMIN_PASSWORD
#    - Realm: logistica
#
# 5. SEGURIDAD:
#    - ⚠️ NUNCA uses estas credenciales por defecto en producción
#    - ⚠️ Agrega .env a tu .gitignore
#    - ⚠️ Usa variables de entorno del sistema en CI/CD
#
# =============================================================================
